import { VowelSubstitutePipe } from './vowel-substitute.pipe';

describe('VowelSubstitutePipe', () => {
  it('create an instance', () => {
    const pipe = new VowelSubstitutePipe();
    expect(pipe).toBeTruthy();
  });
});
